document.addEventListener('pa:ready',render);function render(){const c=document.getElementById('appContent'),s=PAStore.get(),done=Object.values(s.training||{}).filter(Boolean).length;c.innerHTML=`<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><div class="page-eyebrow">Knowledge base</div><h1 class="page-title">Training center</h1><p class="text-muted mb-0">${PA_TRAINING.length} practical lessons you can apply immediately.</p></div><span class="badge badge-soft fs-6">${done}/${PA_TRAINING.length} complete</span></div><div class="row g-4">${PA_TRAINING.map(t=>{const yes=s.training?.[t.id];return `<div class="col-md-6"><article class="card training-item p-4 h-100"><div class="d-flex gap-3"><div class="scenario-icon"><i class="bi ${t.icon}"></i></div><div class="flex-grow-1"><div class="d-flex justify-content-between"><h2 class="h5">${t.title}</h2><span class="small text-muted">${t.minutes} min</span></div><p class="text-muted">${t.body}</p><button class="btn btn-sm ${yes?'btn-soft':'btn-primary'} training-btn" data-id="${t.id}" ${yes?'disabled':''}><i class="bi ${yes?'bi-check2':'bi-book'} me-1"></i>${yes?'Completed':'Mark as complete'}</button></div></div></article></div>`}).join('')}</div>`;document.querySelectorAll('.training-btn').forEach(b=>b.onclick=()=>{PAStore.completeTraining(b.dataset.id);PAToast('Lesson completed and progress saved.');render()})}
